{
  "name": "rustycad",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:wasm": "wasm-pack build --target web",
    "build:frontend": "cd frontend && npm run build",
    "build:all": "npm run build:wasm && npm run build:frontend",
    "dev": "npm run build:wasm && cd frontend && npm run dev"
  }
}
